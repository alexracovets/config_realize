'use client';

import { useConfiguratorRouteReset } from '@hooks/useConfiguratorRouteReset';

const ConfiguratorRouteResetBridge = () => {
  useConfiguratorRouteReset();

  return null;
};

export { ConfiguratorRouteResetBridge };
